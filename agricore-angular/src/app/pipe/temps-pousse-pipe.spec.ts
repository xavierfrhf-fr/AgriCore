import { TempsPoussePipe } from './temps-pousse-pipe';

describe('TempsPoussePipe', () => {
  it('create an instance', () => {
    const pipe = new TempsPoussePipe();
    expect(pipe).toBeTruthy();
  });
});
