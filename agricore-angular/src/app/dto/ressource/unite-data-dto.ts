export interface UniteDataDto {
  nom: string;       // Valeur enum (ex: 'KILOGRAM') — utilisée comme valeur dans le FormControl
  affichage: string; // Libellé lisible (ex: 'Kg') — affiché dans le select
}
